@model IEnumerable<XArbete.Web.Features.Kennel.ViewModels.PuppyViewModel>


@if (Model.Count() != 0)
{
    <table>
        <tr>
            <th style="width:130px">Namn<span> <i class="material-icons" data-toggle="tooltip" title="Redigeringsbar">edit</i></span></th>
            <th>Kön</th>
            <th>Bild</th>
            <th>Såld</th>
            <th></th>
        </tr>

        @foreach (var dog in Model)
        {
            <tr>
                <td class="thedogid" hidden>@dog.PuppyId</td>
                <td contenteditable="true" class="puppy editor" spellcheck="false">@dog.Name</td>
                <td>@dog.Sex</td>
                <td>@dog.Img</td>
                <td>
                    @if (dog.Sold)
                    {
                        <input type="checkbox" class="form-control puppySold" checked data-id="@dog.PuppyId" />
                    }
                    else
                    {
                        <input type="checkbox" class="form-control puppySold" data-id="@dog.PuppyId" />
                    }
                </td>
                <td>
                    <a href="#deletePuppy" class="delete deletepuppyaction" data-toggle="modal" data-id="@dog.PuppyId" data-groupid="@dog.PuppyGroupId">
                        <i class="material-icons" data-toggle="tooltip" title="Radera">
                            clear
                        </i>
                    </a>
                </td>
            </tr>
        }
    </table>
}
